# 25 键键盘映射规范（15 主键 + 10 半音键）

## 概述

本项目基于 Sky 光遇乐器的 15 键布局，扩展为 25 键全色彩音阶（Chromatic Scale），覆盖 C4 到 C6 的完整音域。

- **音域范围**：MIDI 60 (C4) ~ MIDI 84 (C6)
- **总键数**：25（= 84 - 60 + 1）
- **主键（白键）**：15 个，对应 C 大调自然音
- **半音键（黑键）**：10 个，对应升降音（#）

---

## 完整 25 键排列（按 MIDI 音高升序）

| 序号 | MIDI Pitch | 音名 | 简谱 | 类型 | 说明 |
|------|-----------|------|------|------|------|
| 0 | 60 | C4 | 1 | ⬜ 白键 | 中央 C（低音 do） |
| 1 | 61 | C#4 | 1# | ⬛ 黑键 | — |
| 2 | 62 | D4 | 2 | ⬜ 白键 | 低音 re |
| 3 | 63 | D#4 | 2# | ⬛ 黑键 | — |
| 4 | 64 | E4 | 3 | ⬜ 白键 | 低音 mi |
| 5 | 65 | F4 | 4 | ⬜ 白键 | 低音 fa |
| 6 | 66 | F#4 | 4# | ⬛ 黑键 | — |
| 7 | 67 | G4 | 5 | ⬜ 白键 | 低音 sol |
| 8 | 68 | G#4 | 5# | ⬛ 黑键 | — |
| 9 | 69 | A4 | 6 | ⬜ 白键 | 低音 la |
| 10 | 70 | A#4 | 6# | ⬛ 黑键 | — |
| 11 | 71 | B4 | 7 | ⬜ 白键 | 低音 si |
| 12 | 72 | C5 | 1· | ⬜ 白键 | 中音 do |
| 13 | 73 | C#5 | 1#· | ⬛ 黑键 | — |
| 14 | 74 | D5 | 2· | ⬜ 白键 | 中音 re |
| 15 | 75 | D#5 | 2#· | ⬛ 黑键 | — |
| 16 | 76 | E5 | 3· | ⬜ 白键 | 中音 mi |
| 17 | 77 | F5 | 4· | ⬜ 白键 | 中音 fa |
| 18 | 78 | F#5 | 4#· | ⬛ 黑键 | — |
| 19 | 79 | G5 | 5· | ⬜ 白键 | 中音 sol |
| 20 | 80 | G#5 | 5#· | ⬛ 黑键 | — |
| 21 | 81 | A5 | 6· | ⬜ 白键 | 中音 la |
| 22 | 82 | A#5 | 6#· | ⬛ 黑键 | — |
| 23 | 83 | B5 | 7· | ⬜ 白键 | 中音 si |
| 24 | 84 | C6 | 1·· | ⬜ 白键 | 高音 do |

---

## 黑键位置规律

每个八度内，黑键分布在以下位置（E-F 和 B-C 之间无黑键）：

```
八度内结构（以 C 为起点）：
C  C#  D  D#  E  F  F#  G  G#  A  A#  B
白  黑  白  黑  白  白  黑  白  黑  白  黑  白
```

**无黑键的位置**（相邻两个白键之间只差 1 个半音）：
- E 和 F 之间（MIDI 64→65, 76→77）
- B 和 C 之间（MIDI 71→72, 83→84）

---

## 代码实现参考

### 常量定义

```typescript
const MIN_PITCH = 60;  // C4
const MAX_PITCH = 84;  // C6
const PITCH_RANGE = 25; // 总行数 / 总键数

// 15 个白键的 MIDI 值
const WHITE_KEY_PITCHES = [
  60, 62, 64, 65, 67, 69, 71,  // C4 D4 E4 F4 G4 A4 B4
  72, 74, 76, 77, 79, 81, 83,  // C5 D5 E5 F5 G5 A5 B5
  84                             // C6
];

// 10 个黑键的 MIDI 值
const BLACK_KEY_PITCHES = [
  61, 63,         // C#4 D#4
  66, 68, 70,     // F#4 G#4 A#4
  73, 75,         // C#5 D#5
  78, 80, 82      // F#5 G#5 A#5
];
```

### 判断是否为黑键

```typescript
function isBlackKey(pitch: number): boolean {
  const semitone = pitch % 12;
  // 黑键对应的半音位置：1(C#), 3(D#), 6(F#), 8(G#), 10(A#)
  return [1, 3, 6, 8, 10].includes(semitone);
}
```

### 15 键索引 ↔ MIDI 音高互转

```typescript
// Sky 光遇原版 15 键索引（0~14）→ MIDI 音高
const SKY_15_KEY_TO_MIDI: number[] = [
  60, 62, 64, 65, 67,  // 键 0-4:  C4 D4 E4 F4 G4
  69, 71, 72, 74, 76,  // 键 5-9:  A4 B4 C5 D5 E5
  77, 79, 81, 83, 84   // 键 10-14: F5 G5 A5 B5 C6
];

// MIDI 音高 → 15 键索引（仅白键有效，黑键返回 -1）
function midiToSky15KeyIndex(pitch: number): number {
  const index = SKY_15_KEY_TO_MIDI.indexOf(pitch);
  return index; // -1 表示该音高不在 15 键范围内
}
```

### 25 键索引 ↔ MIDI 音高互转

```typescript
// 25 键索引（0~24）→ MIDI 音高（直接偏移）
function keyIndexToMidi(index: number): number {
  return MIN_PITCH + index; // index 0 = pitch 60, index 24 = pitch 84
}

// MIDI 音高 → 25 键索引
function midiToKeyIndex(pitch: number): number | null {
  const index = pitch - MIN_PITCH;
  if (index < 0 || index >= PITCH_RANGE) return null;
  return index;
}
```

### 音高 → 简谱显示

```typescript
function pitchToSimpleNumber(pitch: number): string {
  const baseC4 = 60;
  const diff = pitch - baseC4;
  const semitone = ((diff % 12) + 12) % 12;
  const scale: Record<number, string> = {
    0: '1', 1: '1#', 2: '2', 3: '2#', 4: '3',
    5: '4', 6: '4#', 7: '5', 8: '5#', 9: '6',
    10: '6#', 11: '7'
  };
  return scale[semitone] || '?';
}
```

### 音高 → 标准音名

```typescript
function pitchToNoteName(pitch: number): string {
  const notes = ['C', 'C#', 'D', 'D#', 'E', 'F', 'F#', 'G', 'G#', 'A', 'A#', 'B'];
  const octave = Math.floor(pitch / 12) - 1;
  const noteIndex = pitch % 12;
  return `${notes[noteIndex]}${octave}`;
}
```

---

## 网格视图中的行布局

SkyView 网格共 25 行，每行对应一个半音位置：

```
行索引 0  → pitch 60 (C4)  ⬜
行索引 1  → pitch 61 (C#4) ⬛  ← 黑键行，可用不同背景色区分
行索引 2  → pitch 62 (D4)  ⬜
行索引 3  → pitch 63 (D#4) ⬛
行索引 4  → pitch 64 (E4)  ⬜
行索引 5  → pitch 65 (F4)  ⬜  ← 注意：E-F 之间无黑键
行索引 6  → pitch 66 (F#4) ⬛
行索引 7  → pitch 67 (G4)  ⬜
行索引 8  → pitch 68 (G#4) ⬛
行索引 9  → pitch 69 (A4)  ⬜
行索引 10 → pitch 70 (A#4) ⬛
行索引 11 → pitch 71 (B4)  ⬜
行索引 12 → pitch 72 (C5)  ⬜  ← 注意：B-C 之间无黑键
行索引 13 → pitch 73 (C#5) ⬛
行索引 14 → pitch 74 (D5)  ⬜
行索引 15 → pitch 75 (D#5) ⬛
行索引 16 → pitch 76 (E5)  ⬜
行索引 17 → pitch 77 (F5)  ⬜  ← E-F 之间无黑键
行索引 18 → pitch 78 (F#5) ⬛
行索引 19 → pitch 79 (G5)  ⬜
行索引 20 → pitch 80 (G#5) ⬛
行索引 21 → pitch 81 (A5)  ⬜
行索引 22 → pitch 82 (A#5) ⬛
行索引 23 → pitch 83 (B5)  ⬜
行索引 24 → pitch 84 (C6)  ⬜  ← B-C 之间无黑键
```

---

## 键盘快捷键绑定（编辑器参考）

### 主键（直接按）

| 按键 | Pitch | 音名 | 15键索引 |
|------|-------|------|---------|
| Y | 60 | C4 | 0 |
| U | 62 | D4 | 1 |
| I | 64 | E4 | 2 |
| O | 65 | F4 | 3 |
| P | 67 | G4 | 4 |
| H | 69 | A4 | 5 |
| J | 71 | B4 | 6 |
| K | 72 | C5 | 7 |
| L | 74 | D5 | 8 |
| ; | 76 | E5 | 9 |
| N | 77 | F5 | 10 |
| M | 79 | G5 | 11 |
| , | 81 | A5 | 12 |
| . | 83 | B5 | 13 |
| / | 84 | C6 | 14 |

### 半音键（Shift + 按键）

| 按键 | Pitch | 音名 | 对应关系 |
|------|-------|------|---------|
| Shift+Y | 61 | C#4 | C4 的升音 |
| Shift+U | 63 | D#4 | D4 的升音 |
| Shift+O | 66 | F#4 | F4 的升音 |
| Shift+P | 68 | G#4 | G4 的升音 |
| Shift+H | 70 | A#4 | A4 的升音 |
| Shift+K | 73 | C#5 | C5 的升音 |
| Shift+L | 75 | D#5 | D5 的升音 |
| Shift+N | 78 | F#5 | F5 的升音 |
| Shift+M | 80 | G#5 | G5 的升音 |
| Shift+, | 82 | A#5 | A5 的升音 |

> **注意**：E(64/76) 和 B(71/83) 没有对应的 Shift 升音键，因为 E→F 和 B→C 本身只差 1 个半音。

---

## 从 15 键扩展到 25 键的改动要点

1. **数据层无需改动**：`pitch` 字段本身就是 MIDI 值，天然支持 60~84 所有半音
2. **网格/视图层**：将行数从 15 改为 25（`PITCH_RANGE = 25`），行索引 = `pitch - 60`
3. **输入层**：增加 Shift 组合键绑定 10 个黑键音高
4. **视觉区分**：黑键行使用不同背景色（如深灰 `#1e1f23`），白键行用浅色（如 `#2a2b30`）
5. **标签显示**：使用 `pitchToSimpleNumber()` 或 `pitchToNoteName()` 显示每行标签
6. **兼容性**：原有 15 键数据（只含白键 pitch）在 25 键网格中正常显示，无需迁移
